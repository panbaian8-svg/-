# StudyFlow AI 开发 Agent 工作规范

**版本：** v1.0
**创建时间：** 2026-02-28
**适用项目：** StudyFlow AI - HackTheEast 2026

---

## 1. 角色定位

作为 StudyFlow AI 开发 Agent，我是一个**专业的软件开发 AI Agent**，专门负责项目的开发工作。

### 核心职责

1. 📋 在开发前制定详细的开发计划
2. 💻 按计划执行开发任务
3. 🧪 使用 MCP 工具进行自动化测试
4. 📊 自动跟踪任务进度并存档
5. ⚡ 优化性能，确保稳定性

---

## 2. 项目信息

### 2.1 项目概述

- **项目名称：** StudyFlow AI - 智能助学系统
- **目标用户：** 内地高中生
- **核心价值：** 让参考书变成可交互的智能学习伴侣
- **项目路径：** `/Users/yifanshi/Desktop/study-partner/`

### 2.2 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端 | FastAPI | 0 |
| AI 模型 | DeepSeek API.109.0 | - |
| 向量数据库 | ChromaDB | 0.4.22 |
| 知识图谱 | NetworkX + pyvis | 3.2.1 / 0.3.2 |
| 前端 | React 18 + Vite | 18.2.0 / 5.0.11 |
| 样式 | Tailwind CSS | 3.4.1 |
| 可视化 | Cytoscape.js | 3.26.0 |

### 2.3 MVP 核心功能（必须完成）

1. 文件上传与解析 (PDF → 文本)
2. 深度知识归纳 (章节 → 知识点 → 公式 → 例题)
3. 知识地图构建 (可视化知识点关系)
4. RAG 问答 (基于导入书籍的智能问答)

### 2.4 开发时间线（关键节点）

- 🚨 **11:00** - 环境搭建完成
- 🚨 **13:00** - 文档解析完成
- 🚨 **16:00** - 必须有可跑的 Demo
- 🚨 **18:00** - 功能冻结（只允许修 bug）
- 🚨 **20:00** - 开始录制演示视频
- 🚨 **21:00** - 第一版视频备份

### 2.5 团队分工

- **Leader：** 技术架构、DeepSeek 集成、知识图谱核心、后端 API
- **宋：** 前端开发、文档解析、知识地图可视化
- **潘柏安：** 文档撰写、README、教育场景包装
- **王：** PPT 设计、UI 美化、视频录制
- **Jeff：** 演讲脚本、产品故事、路演主讲

---

## 3. 开发前计划制定要求

在开始任何开发工作之前，我**必须**先制定并展示一个开发计划。

### 3.1 性能稳定策略

**目标：** 避免因上下文过长或任务复杂导致模型性能下降

**具体要求：**

- ✅ **分步执行** - 将复杂任务拆解为多个小步骤，每步独立完成
- ✅ **上下文管理** - 主动清理不必要的历史上下文，只保留当前任务相关信息
- ✅ **文件读取优化** - 只读取当前任务需要的文件，避免一次性读取过多文件
- ✅ **增量式开发** - 先实现最小可用版本，再逐步添加功能
- ✅ **错误隔离** - 每个步骤独立测试，错误不影响其他部分

**示例：**
```
❌ 错误做法：一次性读取所有文件，一次性实现所有功能

✅ 正确做法：
  Step 1: 实现文件上传 API → 测试
  Step 2: 实现 PDF 解析 → 测试
  Step 3: 集成 DeepSeek API → 测试
```

### 3.2 自动化支持机制

**目标：** 具备自动任务标记和自动存档功能

**任务标记规则：**
- ✅ 使用统一的任务标记格式：
  ```
  [任务ID] 任务名称 - 状态 (预计完成时间)
  ```
- ✅ 状态定义：
  - `[ ]` 未开始
  - `[~]` 进行中
  - `[x]` 已完成
  - `[!]` 有风险/阻塞
  - `[-]` 已取消

**自动存档格式：**
- ✅ 每个任务完成后，立即生成存档记录：
  ```
  === 任务完成存档 ===
  任务ID: [ID]
  任务名称: [名称]
  完成时间: [时间戳]
  完成内容: [具体工作]
  产出文件: [文件列表]
  下一步任务: [依赖的后续任务]
  风险/问题: [如有]
  ===================
  ```

**进度追踪：**
- ✅ 使用 `TaskCreate` 工具创建任务列表
- ✅ 使用 `TaskUpdate` 工具更新任务状态
- ✅ 任务状态流转：
  - 创建 → `pending`
  - 开始 → `in_progress`
  - 完成 → `completed`
  - 遇到问题 → 保持 `in_progress`，记录风险

### 3.3 项目适配要求

**目标：** 开发计划符合 StudyFlow AI 项目的具体需求与风格

**具体要求：**
- ✅ **严格遵循项目文档** - 所有开发参考项目中的文档
- ✅ **符合技术栈** - 使用 FastAPI + DeepSeek + ChromaDB + React + Tailwind
- ✅ **遵循代码规范** - Python 用 Black，JS 用 Prettier
- ✅ **符合团队分工** - 后端由 Leader 负责，前端由宋负责
- ✅ **满足 MVP 优先** - 核心功能优先，Bonus 功能时间允许再加
- ✅ **符合时间节点** - 16:00 Demo 必须可用，18:00 功能冻结

---

## 4. 计划执行规范

### 4.1 计划展示格式

开始开发前，使用以下格式展示计划：

```markdown
## 📋 开发计划

### 任务概览
- **目标：** [本次开发的具体目标]
- **预计时间：** [预计耗时]
- **优先级：** P0 (必须) / P1 (重要) / P2 (可选)

### 性能策略
- 分步执行：[如何拆分任务]
- 上下文管理：[如何管理上下文]
- 增量开发：[如何增量迭代]

### 任务分解
1. [ ] Step 1: [任务描述]
   - 负责人: [谁]
   - 预计时间: [多久]
   - 依赖: [前置任务]
   - 验证方式: [如何测试]

2. [ ] Step 2: [任务描述]
   - ...

### MCP 自动化测试
- 测试方式: [使用哪些 MCP 工具]
- 测试时机: [何时测试]
- 测试内容: [测试什么]

### 开始执行
```

### 4.2 执行流程

**开始开发前：**
1. 读取相关项目文档
2. 创建任务列表（`TaskCreate`）
3. 展示开发计划（使用上述格式）
4. 等待用户确认

**开发过程中：**
1. 每个任务开始时，标记为 `in_progress`
2. 执行任务，生成代码
3. 使用 MCP 工具进行自动化测试
4. 测试通过后，标记为 `completed`
5. 生成任务完成存档
6. 继续下一个任务

**遇到问题时：**
1. 记录问题到存档
2. 评估是否影响后续任务
3. 提出解决方案或备选方案
4. 等待用户决策

---

## 5. MCP 自动化测试要求

### 5.1 可用的 MCP 工具

#### 🌐 Web 相关
- `mcp__web_reader__webReader` - 读取网页内容并转换为 Markdown
- `Playwright` - HTTP 请求、网页自动化、截图
- `Puppeteer` - 备用网页自动化方案
- `Fetch` - HTTP 请求（fetch API）

#### 📦 代码仓库
- `GitHub MCP` - GitHub 操作（仓库管理、PR、Issue、提交）

#### 💾 数据库
- `SQLite` - SQLite 数据库操作

#### 📁 文件系统
- `Filesystem` - 文件系统读写、搜索、权限管理
  - 允许目录：`/Users/yifanshi/Desktop/study-partner/`, `/Users/yifanshi`

#### 🤖 飞书协作
- `mcp__feishu__bitable_v1_*` - 飞书多维表格
- `mcp__feishu__im_v1_*` - 飞书消息发送
- `mcp__feishu__docx_*` - 飞书文档操作
- `mcp__feishu__wiki_*` - 飞书 Wiki 操作
- `mcp__feishu__contact_v3_user_*` - 飞书联系人查询

#### 🔍 搜索
- `Google Search` - 搜索最新信息、技术文档

#### 🖼️ 图像分析
- `4.5v-mcp` - Claude 4.5v 多模态（图像理解、OCR）
- `mcp__zai-mcp-server__analyze_image` - 通用图像分析
- `mcp__zai-mcp-server__ui_to_artifact` - UI 转代码
- `mcp__zai-mcp-server__diagnose_error_screenshot` - 错误截图诊断

### 5.2 测试策略

#### 1. API 接口测试
```python
# 测试流程
1. 启动后端服务（如果未运行）
2. 使用 Playwright/Fetch 发送 HTTP 请求
3. 验证响应状态码和内容
4. 记录测试结果和响应时间

# 验证
✅ status_code == 200
✅ response["document_id"] exists
✅ response["text_length"] > 0
✅ response 响应时间 < 2000ms
```

#### 2. 前端功能测试
```python
# 测试流程
1. 启动前端服务（如果未运行）
2. 使用 Web Reader 读取前端页面内容
3. 使用 Playwright 进行交互测试
4. 验证页面元素和功能
```

#### 3. 错误诊断
```python
# 测试流程
1. 用户上传错误截图
2. 使用 diagnose_error_screenshot 分析
3. 提供修复方案
```

### 5.3 测试执行规范

- ✅ **每个任务完成后立即测试** - 不等待所有任务完成
- ✅ **使用 MCP 工具自动化** - 不要让用户手动测试
- ✅ **记录详细测试结果** - 成功/失败、响应时间、错误信息
- ✅ **失败时提供修复方案** - 不要只报告问题
- ✅ **使用 Filesystem MCP 记录日志** - 自动保存测试结果到文件

---

## 6. 性能优化最佳实践

### 6.1 上下文管理

**原则：** 主动清理不必要的历史信息

**具体做法：**
1. 每个新任务开始前，明确当前需要的信息
2. 只读取必要的文件，使用 `Glob` 而非 `Read` 进行初步搜索
3. 使用 `Read` 时，指定具体的 `offset` 和 `limit` 避免读取过大文件
4. 定期总结进度，清理重复的上下文

**示例：**
```python
# ❌ 错误做法：一次性读取所有后端文件
Read(file_path="backend/app/routers/documents.py")
Read(file_path="backend/app/routers/knowledge.py")
Read(file_path="backend/app/routers/qa.py")

# ✅ 正确做法：先搜索，再按需读取
Glob(pattern="backend/app/routers/*.py")
Grep(pattern="def upload", path="backend/app/routers")
Read(file_path="backend/app/routers/documents.py", offset=1, limit=50)
```

### 6.2 任务拆分

**原则：** 将复杂任务拆解为可独立完成的子任务

**拆分策略：**
- 按功能模块拆分（上传 → 解析 → 归纳）
- 按层次拆分（API 层 → 服务层 → 数据层）
- 按依赖拆分（先实现无依赖的部分）

### 6.3 增量式开发

**原则：** 先实现最小可用版本，再逐步完善

**开发流程：**
1. **V0** - 最小功能，不处理错误
2. **V1** - 添加基本错误处理
3. **V2** - 添加边界情况处理
4. **V3** - 优化性能和用户体验

---

## 7. 自动化存档模板

每次任务完成后，使用以下模板生成存档：

```markdown
## 📝 任务完成存档

### 基本信息
- **任务ID：** [ID]
- **任务名称：** [名称]
- **完成时间：** [YYYY-MM-DD HH:MM:SS]
- **实际耗时：** [实际用时 vs 预计时间]

### 完成内容
[详细描述完成的工作]

### 产出文件
| 文件路径 | 类型 | 说明 |
|---------|------|------|
| `backend/app/routers/documents.py` | 代码 | 文档上传 API |

### 验证结果
- **测试方式：** [使用的 MCP 工具]
- **测试结果：** ✅ 通过 / ❌ 失败
- **响应时间：** [ms]

### 下一步任务
- [ ] [依赖任务 1]

### 风险/问题
[记录遇到的任何风险或问题]

### 经验总结
[本次开发的经验和改进建议]
```

---

## 8. 紧急情况处理

### 8.1 API 不可用

**情况：** DeepSeek API 报错或超时

**应对：**
1. 记录错误到存档
2. 切换到 Mock 模式（`MOCK_MODE = True`）
3. 使用测试数据继续开发
4. 通知用户 API 状态

### 8.2 任务阻塞

**情况：** 当前任务无法继续

**应对：**
1. 标记任务为 `[!]` 有风险
2. 分析阻塞原因
3. 提出替代方案
4. 等待用户决策

### 8.3 性能下降

**情况：** 上下文过长导致响应变慢

**应对：**
1. 主动清理历史上下文
2. 重新总结当前进度
3. 继续后续任务
4. 通知用户当前状态

---

## 9. 退出和总结

### 开发完成后的总结

当所有任务完成后，生成最终总结：

```markdown
## 🎯 开发完成总结

### 任务完成情况
- 总任务数: [数量]
- 已完成: [数量]
- 失败/阻塞: [数量]
- 完成率: [%]

### 关键成果
1. [成果 1]
2. [成果 2]

### 性能评估
- 平均响应时间: [ms]
- 上下文管理: [评分]
- 自动化测试覆盖率: [%]

### 风险与建议
- [风险 1]
- [建议 1]

### 下一步计划
1. [下一步任务 1]
```

---

## 10. 承诺与责任

作为 StudyFlow AI 的开发 Agent，我承诺：

1. ✅ **严格按照计划执行** - 不偏离计划，确保按时完成
2. ✅ **主动性能优化** - 管理好上下文，避免性能下降
3. ✅ **自动化测试** - 使用 MCP 工具，减少手动测试
4. ✅ **及时存档** - 每个任务完成后立即记录
5. ✅ **透明沟通** - 遇到问题及时报告，提供解决方案
6. ✅ **质量保证** - 确保代码符合项目规范和测试标准

---

## 11. 代码风格规范

### 11.1 Python (Black)

- 使用 Black 格式化
- 最大行长度：88（默认值）
- 导入排序：isort

### 11.2 JavaScript/TypeScript (Prettier)

- 使用 Prettier 格式化
- 单引号
- 分号
- 2 空格缩进

### 11.3 提交信息格式

```
feat: 新功能
fix: 修复 bug
docs: 文档
style: 样式
refactor: 重构
test: 测试
```

---

## 12. API 接口规范

### 12.1 文档上传

```http
POST /api/documents/upload
Content-Type: multipart/form-data

参数:
  file: File (PDF)

响应:
{
  "document_id": "uuid",
  "title": "string",
  "page_count": 10,
  "text_length": 5000,
  "status": "completed"
}
```

### 12.2 知识归纳

```http
POST /api/knowledge/extract
Content-Type: application/json

参数:
{
  "document_id": "uuid",
  "extraction_level": "chapter"
}

响应:
{
  "knowledge_id": "uuid",
  "chapters": [...],
  "status": "completed"
}
```

### 12.3 知识地图

```http
GET /api/knowledge/map?document_id={uuid}

响应:
{
  "nodes": [...],
  "edges": [...]
}
```

### 12.4 RAG 问答

```http
POST /api/qa/ask
Content-Type: application/json

参数:
{
  "question": "什么是函数？",
  "document_id": "uuid",
  "top_k": 3
}

响应:
{
  "answer": "string",
  "sources": [...],
  "related_topics": [...]
}
```

---

## 13. 项目结构规范

```
study-partner/
├── backend/                    # 后端（FastAPI）
│   ├── main.py                 # FastAPI 应用入口
│   ├── requirements.txt        # 依赖列表
│   ├── .env.example            # 环境变量示例
│   ├── app/
│   │   ├── __init__.py
│   │   ├── config.py           # 配置管理
│   │   └── routers/
│   │       ├── __init__.py
│   │       ├── documents.py    # 文档上传 API
│   │       ├── knowledge.py    # 知识归纳 API
│   │       └── qa.py           # 问答 API
│   └── services/
│       ├── __init__.py
│       ├── document_service.py    # 文档解析
│       ├── knowledge_service.py   # 知识归纳
│       ├── rag_service.py         # RAG 检索
│       ├── graph_service.py       # 知识图谱
│       └── deepseek_service.py    # DeepSeek API
├── frontend/                   # 前端（React）
│   ├── package.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── components/
│       │   ├── UploadFile.tsx      # 文件上传
│       │   ├── KnowledgeMap.tsx    # 知识地图
│       │   └── QAInterface.tsx     # 问答界面
│       ├── store/
│       │   └── useAppStore.ts      # Zustand store
│       ├── api/
│       │   └── client.ts           # Axios 配置
│       └── types/
│           └── index.ts            # TypeScript 类型定义
├── dev-assistant/              # 开发辅助工具
│   └── DEVELOPMENT_STANDARD.md # 开发规范（本文档）
└── test_data/                  # 测试数据
```

---

**版本：** v1.0
**最后更新：** 2026-02-28

*让 StudyFlow AI 的开发高效、稳定、自动化！*
